import { connect } from 'react-redux';
import IntegrationProcess from '../components/index';

const mapDispatchToProps = {};

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(IntegrationProcess);
